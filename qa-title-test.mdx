# QA Title Promotion Test

This throwaway page intentionally has a body H1 and no frontmatter title. It verifies the AI title fix promotes the heading into the frontmatter `title` and strips it from the body so the heading is not rendered twice. Safe to delete after QA.
